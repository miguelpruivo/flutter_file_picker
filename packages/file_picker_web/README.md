# file_picker_web

The Web implementation of [`file_picker`](https://pub.dev/packages/file_picker).

## Usage

This package is endorsed, which means you can simply add `file_picker` to your `pubspec.yaml`, and it will automatically use `file_picker_web` when building for the web.
