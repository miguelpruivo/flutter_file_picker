name: file_picker_example
description: An example of how to use the file_picker plugin.
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

formatter:
  page_width: 80

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^12.0.0-beta.8
  file_picker_android: ^1.0.0
  file: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true